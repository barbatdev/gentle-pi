# Agent-runtime decision log

Append new durable decisions only. Historical decision dates were not preserved in the ledger, so the entries below are marked `Recorded 2026-08-26`; that is the record date, not a claimed decision date.

| ID | Record date | Decision | Rationale | Affected units | Supersedes or tombstones |
| --- | --- | --- | --- | --- | --- |
| D-001 | Recorded 2026-08-26 | Keep three separate parents: #419, #376, and #442. | The roadmap has three distinct scopes and review surfaces. | A1-A7c1, A5a-A5b, B1-B7e | None |
| D-002 | Recorded 2026-08-26 | Publish parents first and children just in time. | Parents remain concise review surfaces while the ledger retains detailed contracts; no bulk child publication follows from a parent state. | All active children | None |
| D-003 | Recorded 2026-08-26 | Keep #419 same-cwd managed execution independent of #376 and peer completion. | A5b consumes A1-A4 one way; #419 does not wait for A5b, and neither parent waits for peer work. | A1-A7c1, A5b, B1-B7e | None |
| D-004 | Recorded 2026-08-26 | Tombstone A6b1 under YAGNI. | The speculative process-host authority adapter is outside the same-cwd managed-execution scope and has no replacement contract. | A6b1 | A6b1 is tombstoned; no successor |
| D-005 | Recorded 2026-08-26 | Make #376 one-way after delivered A1-A4. | Target-cwd integration consumes the delivered managed-execution foundation without blocking #419 completion. | A1-A4, A5b | A5 split into A5a and A5b |
| D-006 | Recorded 2026-08-26 | Bound peer v1 to local same-user, process-local operation with no recovery boundary. | The peer roadmap explicitly excludes cross-machine delivery, durable mailbox/history, and same-user malicious-process isolation claims. | B1-B7e | None |
| D-007 | Recorded 2026-08-26 | Split peer authority: B2 owns trust, credentials, and revocation decisions; B6 owns delivery admission, ACKs, dedupe, queue, and turn state. | This prevents platform and credential slices from acquiring delivery authority. | B2a, B2b1, B2b2, B6a, B6b1, B6c, B6d | B2 and B6 tombstones preserve the earlier combined scopes |
| D-008 | Recorded 2026-08-26 | Do not claim selective dequeue for Pi follow-up entries after handoff. | The ledger records no supported selective removal for the exact `deliverAs:'followUp'` entry; cancellation stays correlated and bounded. | B2b2, B6a, B6b1, B6c, B6d | None |
| D-009 | Recorded 2026-08-26 | Limit peer-principal authority to exactly three wrapper fingerprints. | The allowlist is the immutable fingerprints for `gentle_inspect_project`, `gentle_list_agents`, and `gentle_send_message`; name-only authority is forbidden. | B6b1, B7a, B7b | None |
| D-010 | Recorded 2026-08-26 | Preserve tombstones and invalidate forecasts whenever a work unit splits. | Splits retain decision history and require regenerated counts, topology, hashes, and publication evidence before new authorization. | A5, A6, A7, B2, B4, B5, B6, B7 and successors | Existing tombstones retain successor mappings |
| D-011 | Recorded 2026-08-26 | Treat this ledger branch as the source of truth for the internal roadmap. | The manifest, tracker, work-unit contracts, tombstones, and integrity records are the durable internal record; public parents stay concise. | Entire ledger | None |
| D-013 | Recorded 2026-08-27 | Keep A2 draft until dependency boundaries and governance are corrected. | Independent Pi reviews using MiMo 2.5, DeepSeek V4 Flash, Qwen 3.8 Flash, and GLM 5.3 all returned REVISE: preserve resolver/validation scope, remove mutable governance, revalidate #379/#354, clarify #447/#376/#448/#449 boundaries, and define fail-closed output before publication review. | A2, #379, #354, #447, #376, #448, #449 | No publication or implementation authorization; Part 2 reconciliation remains unapproved |
| D-014 | Recorded 2026-08-27 | Register #448/#449 as public-contract snapshots with unproven native linkage; replace A2 draft bytes and register four proposal-validation artifacts. | A2 remains draft/REVISE with G01-G08 open; 29/29 proves proposal self-consistency only. Terminal executable checks passed, but architecture remains REVISE: no isolated project-only/global-only discovery mutation fixtures; no explicit rejected-authority and mismatched-context internal-read cases for both success/aggregate, plus no non-aggregate public failure through `resolvePublic`; and no supplied tool authority that explicitly rejects one requested tool. Dependencies and state are unchanged; no implementation, public issue publication, or approval is claimed. Snapshots record observed remote bodies/states at this update, not permanent facts. | A2, #448, #449 | No publication or implementation authorization |
| D-015 | Recorded 2026-08-27 | Persist the validated A2 G01-G08 closure program. | Baseline is 0/8 production gaps closed despite schema/evidence groundwork; milestones run 0→100 and require runtime evidence, terminal review, and separately authorized #419 publication. Automatic happy-path execution uses one candidate-bound mutation authorization, a 400-line split threshold, and strict TDD for executable units. #379/#354 classification and G07 policy remain maintainer decisions; this record chooses neither. | A2, G01-G08, #379, #354, #419 | No implementation, public issue publication, or upstream mutation authorization is inferred |
| D-016 | Recorded 2026-08-28 | Select Separate semantics for A2 #379/#354 classification. | #379 remains the formal implementation-start containment gate/dependency; #354 is removed from A2’s technical dependency graph and retained only as delivery/install acceptance alignment after technical closure. No schema expansion; A2 remains draft/REVISE with G01–G08 open; no implementation or publication approval. Next decision: G07. | A2, #379, #354, G07 | None |
| D-017 | Recorded 2026-08-28 | Select G07 option B: maximum 16 normalized candidates per source kind and 64 total per managed resolution. | Count after passive structural parsing and tuple normalization, before definition validation or external adapter iteration; malformed ingress counts as one rejected ingress item but never becomes a candidate, and exact duplicate structural tuples count once. At 17 for one source or 65 total, fail closed with `candidate-limit-exceeded`; public metrics expose only total count, configured total limit, triggering source category when applicable, configured per-source limit, and outcome. No dynamic configuration, deployment override, or unbounded fallback is authorized; rollback is only to a previously approved bounded policy. This completes milestone 10% with D-016, while G07 executable boundary, operational, metric, and rollback evidence remains pending and production gaps closed remain 0/8. | A2, G07, #379, #354, #419 | No implementation, publication, or upstream mutation authorization; next dependency-ready action is H1: verify/deliver #379 containment gate (PR #387 or verified successor) before technical A2 implementation |
| D-018 | Recorded 2026-08-29 | Assign G02 structured-selector transport ownership to the external subagent runtime; Gentle Pi owns managed-adapter consumption only. | The external runtime must deliver a backward-compatible singular structured-selector ABI alongside unchanged legacy strings. Exact identity is `{source, owner?, normalizedName}`; exact selectors cannot be redirected by alias or model overrides, and managed resolution has no name-only or last-write-wins fallback. Plural `agents` behavior is undecided: managed exact fan-out fails closed until the external owner confirms it. | A2, G02, #379, #419 | Owner acceptance and versioned external delivery are required; G02 is `BLOCKED_EXTERNAL`, H1 remains the implementation gate, and no implementation, issue activity, publication, or upstream mutation is authorized |
